@extends('layouts.control')
@section('title', __('journal.editorial_board'))
@push('styles')<link rel="stylesheet" href="{{ asset('assets/css/iuoamc-journal-1.0.0.css') }}">@endpush
@section('content')<div class="journal-control">@include('control.journal._nav')
<section class="page-heading"><div><span class="eyebrow">MCIJ / VERIFIED APPOINTMENTS</span><h1>{{ __('journal.editorial_board') }}</h1><p>{{ __('journal.editorial_board_intro') }}</p></div><a class="primary-action" href="{{ route('journal.control.editorial-members.create',['locale'=>app()->getLocale()]) }}">{{ __('journal.add_editorial_member') }}</a></section>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<section class="data-card"><div class="table-wrap"><table><thead><tr><th>{{ __('journal.name') }}</th><th>{{ __('journal.editorial_role') }}</th><th>{{ __('journal.affiliation') }}</th><th>{{ __('journal.status') }}</th><th>{{ __('journal.consent') }}</th><th>{{ __('journal.action') }}</th></tr></thead><tbody>@forelse($members as $member)<tr><td><strong>{{ $member->name }}</strong><small class="table-note"><bdi dir="ltr">{{ $member->orcid ?: '—' }}</bdi></small></td><td>{{ __('journal.board_roles.'.$member->role) }}</td><td>{{ $member->localized('affiliation') ?: '—' }}</td><td><span class="status-badge status-{{ $member->status }}">{{ __('journal.board_statuses.'.$member->status) }}</span></td><td>{{ $member->consented_at?->format('Y-m-d') ?: '—' }}</td><td>@if($member->status!=='archived')<a class="table-action" href="{{ route('journal.control.editorial-members.edit',['locale'=>app()->getLocale(),'editorial_member'=>$member]) }}">{{ __('journal.edit') }}</a>@else—@endif</td></tr>@empty<tr><td colspan="6">{{ __('journal.no_editorial_members') }}</td></tr>@endforelse</tbody></table></div>@include('control.organizations._pagination',['paginator'=>$members])</section></div>@endsection
